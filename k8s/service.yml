apiVersion: v1
kind: Service
metadata:
  name: afdbhd
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "afdbhd"
    port: 8080
  selector:
    app: afdbhd
