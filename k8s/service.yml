apiVersion: v1
kind: Service
metadata:
  name: wqfavn
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "wqfavn"
    port: 8080
  selector:
    app: wqfavn
