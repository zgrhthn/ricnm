apiVersion: v1
kind: Service
metadata:
  name: ricnm
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "ricnm"
    port: 8080
  selector:
    app: ricnm
